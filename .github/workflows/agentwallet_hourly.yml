# name: AgentWallet Hourly Full Test

# on:
#   schedule:
#     - cron: '0 * * * *'  # Every hour, 24x per day
#   workflow_dispatch:     # Manual trigger

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
    
#     steps:
#       - uses: actions/checkout@v4
      
#       - name: Set up Python 3.11
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.11'
      
#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install requests
      
#       - name: Run AgentWallet full feature test
#         env:
#           AGENTWALLET_API_TOKEN: ${{ secrets.AGENTWALLET_API_TOKEN }}
#           AGENTWALLET_USERNAME: ${{ secrets.AGENTWALLET_USERNAME }}
#           TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
#           TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
#         run: python3 test_full_features.py
      
#       - name: Upload test results
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: test_results_${{ github.run_number }}
#           path: test_results.json
#           retention-days: 7
      
#       - name: Notify on failure
#         if: failure()
#         env:
#           TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
#           TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
#         run: |
#           python3 << 'EOF'
#           import requests
#           import os
          
#           token = os.environ.get("TELEGRAM_BOT_TOKEN")
#           chat = os.environ.get("TELEGRAM_CHAT_ID")
          
#           if token and chat:
#             msg = "⚠️ AgentWallet test FAILED - Check workflow logs"
#             requests.post(f"https://api.telegram.org/bot{token}/sendMessage",
#                          json={"chat_id": chat, "text": msg}, timeout=10)
#           EOF
